<ul class="thumbnails">
    <% for p in @gallery.photos %>
        <li>
            <%= link_to(image_tag(p.img_url(:thumb), :size => "80x80", :title => p.read_attribute(:img)), p.img_url, :class => 'thumbnail') %>
        </li>
    <% end %>
</ul>
