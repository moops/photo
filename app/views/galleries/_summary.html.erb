<div class="gallery-summary row">
    <div class="col-2">
        <%= default_photo(gallery) %>
    </div>
    <div class="col-10 gallery-summary-text">
        <%= link_to(gallery.name, gallery) %><br/>
        <%= gallery.photo_count %> photos :: <%= gallery.gallery_on.strftime("%b %d, %Y") %>
    </div>
</div>
